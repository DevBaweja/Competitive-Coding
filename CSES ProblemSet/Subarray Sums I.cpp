#include <bits/stdc++.h>
using namespace std;

/*
int main()
{
    int n, x;
    cin >> n >> x;

    vector<int> A(n);
    for (int i = 0; i < n; i++)
        cin >> A[i];

    int count = 0;

    int low = 0, high = 0;
    while (low < n && high < n)
    {
        int sum = 0;
        for (int k = low; k <= high; k++)
            sum += A[k];
        if (sum == x)
            count++;
        if (sum > x)
            low++;
        else
            high++;
    }
    cout << count << endl;
    return 0;
}
*/

int main()
{
}