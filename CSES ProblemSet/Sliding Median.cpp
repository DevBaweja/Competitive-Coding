#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
int main()
{
    int n, k;
    cin >> n >> k;
    vector<ll> A(n);
    for (int i = 0; i < n; i++)
        cin >> A[i];

    int low = 0, high = k - 1;
    int value = A[0];
    for (int i = 0; i < n; i++)
    {
    }
}